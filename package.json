{
  "name": "retailcode-sdk",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build && cp packages/web/dist/retailcode.iife.global.js examples/web/",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "mock": "node examples/mock-server/server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsup": "^8.0.2"
  }
}
